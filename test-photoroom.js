
const fetch = require('node-fetch');
const fs = require('fs');

async function test() {
  const file = fs.readFileSync('test.png'); // Need a test image, let's use the one generated recently
  // Let's just create a generic test script that takes an image and sends it
}

